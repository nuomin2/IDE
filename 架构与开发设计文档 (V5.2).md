# Pytools 轻量级网络仿真模块 - 架构与开发设计文档 (V5.2)

> **修订日期**: 2026-05-19 | **修订依据**: 基于当前代码库全量逆向审计 (`Current_Codebase_Report.md`)
> **修订说明**: 移除旧文档中不存在的设计（`IRoutingStrategy`、对象池、`routing_matrix`、`output_config`、`Packet` 类等），全部内容已与实际 C# 和 Python 源码对齐。

## 1. 系统总体架构 (System Architecture)

系统采用**前后端分离的桥接架构 (Bridge Architecture)**。以 Python 作为"前端"配置与表现层，以 C# 作为"后端"高负载核心计算层。两者通过标准输入输出流 (stdio) 配合特定标记的 JSON 字符串进行**双向全双工通信**。

### 1.1 核心分层设计

| 层次 | 核心职责 | 技术栈 / 涉及组件 |
| :--- | :--- | :--- |
| **表现层 (UI)** | 接收用户脚本，展示文本日志与图表。 | Pytools IDE (WPF, AvalonDock, AvalonEdit, Plot Viewer) |
| **配置层 (Python)** | 面向对象的 API 封装，参数校验，JSON 序列化，挂起等待结果，matplotlib CDF 绘图，中文字段翻译，变量环境注入。 | Python 3.x, `matplotlib`, 自定义 `pytools_sim` 库 |
| **通信层 (IPC)** | **运行时生成包装脚本**，拦截 stdout 提取三通道标记（仿真/图表/变量），通过 stdin 注入仿真结果。 | `PythonExecutionService.cs` |
| **仿真内核 (C#)** | 独立 `Task.Run()` 运行，PriorityQueue 离散事件调度，虚拟时钟推进，单跳路由，自适应 CDF 降维统计。 | .NET 8, MathNet.Numerics 5.0, `PriorityQueue<SimEvent, (double, long)>` |

## 2. Python 端设计 (配置层、IPC 交互与自适应结果处理)

Python 端核心任务是为用户提供极简 API，负责与 C# 进程的阻塞式通信，并在运行结束后进行控制台报告打印、CDF 绘图、以及变量区中文字典注入。

### 2.1 领域对象模型 (Domain Models)

**文件**: `Pytools/SDK/pytools_sim.py`

* **`Node` (基类)**: 包含 `id` 和 `name`。
    * **`__repr__`**: 返回 `f"{self.__class__.__name__}(id={self.id!r})"`，使变量区直接呈现 `Host(id='H1')` 格式。
    * **`Host(Node)`**: 主机节点。
    * **`Router(Node)`**: 路由器节点。
* **`Link`**: 包含 `src: str`, `dst: str`, `bw: int` (Mbps), `delay: float` (ms)。
* **`Traffic`**: 流量配置类。
    * 字段: `src`, `dst`, `type`, `interval_mean: float`, `payload_mean: int`, `payload_variance: float` (默认 0.0)。
    * **TCP 禁令** (构造函数内): 若 `type.upper() == "TCP"`，立即 `raise ValueError("当前版本暂不支持 TCP 协议。请使用 UDP。")`。
* **`Simulator`**: 核心编排类，内部维护 `self.nodes: list[Node]`, `self.links: list[Link]`, `self.traffics: list[Traffic]`。

### 2.2 IPC 阻塞挂起与三阶段流程

`Simulator.run(simulation_time, seed=None, summary_only=False)` 的工作流：

**阶段 1 — 下发配置**：构造 JSON payload 并向标准输出打印：
```python
payload = {
    "simulation_time": simulation_time,
    "seed": seed,
    "summary_only": summary_only,
    "nodes": [
        {"id": n.id, "type": n.__class__.__name__} for n in self.nodes
    ],
    "links": [
        {"src": l.src, "dst": l.dst, "bw": l.bw, "delay": l.delay}
        for l in self.links
    ],
    "traffic": [
        {
            "src": t.src, "dst": t.dst, "type": t.type,
            "interval_mean": t.interval_mean,
            "payload_mean": t.payload_mean,
            "payload_variance": t.payload_variance,
        }
        for t in self.traffics
    ],
}
json_string = json.dumps(payload, ensure_ascii=False)
print(f"##SIM_START## {json_string} ##SIM_END##", flush=True)
```
注意: `nodes[].type` 取值 `n.__class__.__name__`，即 `"Host"` 或 `"Router"`。

**阶段 2 — 挂起等待**：
```python
response_str = sys.stdin.readline()
result = json.loads(response_str)
```
Python 进程主动阻塞在 `stdin.readline()`，等待 C# 通过 `WriteStdin()` 注入的响应 JSON。

**阶段 3 — 多通道呈现**：
1. **控制台报告**: 调用 `_print_report(result, simulation_time, summary_only)`，向 IPython Console 打印 ASCII 表格。
2. **CDF 绘图**: 若 `result` 中存在 `cdf_data` 且非空，使用 matplotlib 绘制 CDF 曲线并调用 `plt.show()`（已被 wrapper 脚本劫持为 Base64 输出）。
3. **变量区注入**: 调用 `_translate_keys(result)` 将 C# 英文键映射为中文键，返回 `dict` 供 Variable Explorer 展示。

### 2.3 中文字段翻译 (`_translate_keys`)

C# 返回的英文 key → 中文显示名映射：

| 层级 | 英文 Key | 中文名 |
|------|----------|--------|
| 顶层 | `status` | 状态 |
| 顶层 | `total_packets` | 总发包数 |
| 顶层 | `execution_time_sec` | 内核真实耗时（s） |
| 顶层 | `global_avg_delay_ms` | 全局平均时延（ms） |
| 顶层 | `global_std_dev_ms` | 时延标准差（ms） |
| 顶层 | `flows` | 流量明细 |
| flows 内 | `avg_delay_ms` | 平均时延（ms） |
| flows 内 | `loss_rate` | 丢包率 |
| flows 内 | `peak_queue` | 队列峰值 |

### 2.4 控制台报告格式 (`_print_report`)

```text
==================================================
  仿真完成
==================================================
  状态:              success
  仿真时长 (s):      500
  总发包数:          1513
  内核耗时 (s):      0.045
  平均时延 (ms):     3.835
  时延标准差 (ms):   0.523
  ------------------------------------------------
  [H1->H2]
    平均时延:  12.450 ms
    丢包率:    0.0000
    队列峰值:  42
==================================================
```
注意: 当 `summary_only=True` 时，`flows` 为空字典，流量明细段不打印。

### 2.5 CDF 绘图 (`pytools_sim.py` L141-154)

```python
if "cdf_data" in result and result["cdf_data"]:
    cdf = result["cdf_data"]
    x_delays = cdf.get("x_delays", [])
    y_probs = cdf.get("y_probabilities", [])
    if x_delays and y_probs:
        plt.figure(figsize=(8, 5))
        plt.plot(x_delays, y_probs, marker=".", linestyle="-",
                 color="#1f77b4", linewidth=2)
        plt.title("全局时延累积分布函数 (CDF)", fontsize=12, fontweight="bold")
        plt.xlabel("时延 (ms)", fontsize=10)
        plt.ylabel("累积概率 (Probability)", fontsize=10)
        plt.grid(True, linestyle="--", alpha=0.7)
        plt.tight_layout()
        plt.show()  # 被 wrapper 脚本劫持为 _pytools_show → Base64 输出
```
注意: 当前实现纵轴为小数 (0.0~1.0)，未使用 `PercentFormatter` 转换为百分比。中文字体依赖系统默认字体，未显式配置 `SimHei` 等中文字体回退。

---

## 3. C# 通信层设计 (IPC 管道与包装脚本)

**文件**: `Pytools/Services/PythonExecutionService.cs`

### 3.1 运行时包装脚本生成 (`BuildWrapperScript`)

C# 在执行 Python 脚本前，**动态生成一个临时包装脚本**到 `Path.GetTempPath()/pytools_run_{Guid:N}.py`，执行完毕后通过 `File.Delete(wrapperPath)` 自动清理。

包装脚本的核心逻辑：

```
1. 将 SDK 目录注入 sys.path（import pytools_sim 的依赖）
2. chdir 到用户脚本所在目录
3. 清除用户脚本中的 coding 声明行（避免 exec 语法错误）
4. ★ 猴子补丁: matplotlib.use('Agg') → 替换 plt.show 为 _pytools_show
5. exec(compile(cleaned_user_script, ...), globals()) 执行用户代码
6. 遍历 globals()，过滤掉框架注入变量和模块/函数类型，收集用户变量
7. 打印 ##VAR_DATA_START## {json} ##VAR_DATA_END##
```

**编码声明清除** (L280-283):
```python
_lines = _raw.splitlines(keepends=True)
_cleaned = ''.join(
    '\n' if 'coding:' in line or 'coding=' in line else line
    for line in _lines
)
```

### 3.2 三通道标记解析 (`OnOutputLine`)

**不使用正则表达式**，全部通过 `string.IndexOf(marker, StringComparison.Ordinal)` 进行纯字符串索引匹配。三种标记按以下优先级顺序处理：

#### 通道 A: `##SIM_START##` ... `##SIM_END##` (仿真请求)

匹配 → 截取 JSON → `JsonSerializer.Deserialize<SimulationRequest>` → `TopologyValidator.Validate` 校验 → `Task.Run(() => new SimulatorEngine(request).Run())` 后台执行 → 结果通过 `WriteStdin(json)` 写回 Python。

#### 通道 B: `##PLOT_DATA_START##` ... `##PLOT_DATA_END##` (Base64 图表)

匹配 → 截取 Base64 字符串 → `Convert.FromBase64String` → 触发 `PlotReceived` 事件 → ViewModel 中通过 `Dispatcher.Invoke` 构建 `BitmapImage` 并加入 `PlotList`。

#### 通道 C: `##VAR_DATA_START##` ... `##VAR_DATA_END##` (变量字典)

匹配 → 截取 JSON → 反序列化为 `List<JsonElement>` → 逐元素提取 `n/t/s/v/c` 字段构造 `VariableEntry` → 触发 `VariablesReceived` 事件 → ViewModel 中通过 `Dispatcher.Invoke` 批量加入 `VariableList`。

#### 通道 D: 普通文本透传

未命中任何标记的行，直接触发 `LineReceived` 事件 → 控制台输出。

### 3.3 WriteStdin 与管道安全防护

```csharp
private void WriteStdin(string json)
{
    try
    {
        _process?.StandardInput.WriteLine(json);
        _process?.StandardInput.Flush();
    }
    catch { }  // 静默吞噬：Python 进程意外退出时管道破裂异常
}
```
当 Python 进程被用户强制终止后，管道已关闭，写入操作将触发 `IOException`。此 `try-catch` 确保 WPF IDE 主程序不会因跨进程管道破裂而闪退。

### 3.4 仿真请求处理的三层异常屏障

**第一层 — JSON 反序列化** (L108-149): 整体 `try-catch` 包裹，反序列化失败则构造 `{"status":"error","message":"JSON 解析异常：..."}` 回传。

**第二层 — 拓扑校验失败** (L117-121): `TopologyValidator.Validate()` 返回非 null 错误信息时，构造错误 JSON 回传，不启动仿真引擎。

**第三层 — 仿真引擎异常** (L135-138): `SimulatorEngine.Run()` 内部的异常被捕获，包装为错误 JSON 回传，防止后台 Task 崩溃导致 Python 端永久死锁。

---

## 4. C# 仿真内核设计 (离散事件仿真 DES)

**文件**: `Pytools/Services/SimulatorEngine.cs`

### 4.1 核心数据结构

```csharp
public class SimulatorEngine
{
    private readonly SimulationRequest _request;
    private readonly Random _rng;                                           // 确定性随机数源
    private double _currentTime;                                            // 虚拟时钟
    private long _eventIdCounter;                                           // 全局事件序号
    private readonly PriorityQueue<SimEvent, (double Time, long EventId)> _eventQueue = new();

    private int _totalPackets;                                              // 总发包计数
    private readonly List<double> _allPacketDelays = new();                 // 所有包时延样本
    private readonly Dictionary<string, List<double>> _flowDelays = new();  // 逐流时延
    private readonly Dictionary<string, int> _flowPeakQueues = new();       // 逐流峰值队列
    private readonly Dictionary<string, double> _linkBusyUntil = new();     // 链路忙碌时间轴
}
```

### 4.2 确定性随机数 (`SimulatorEngine` 构造函数)

```csharp
_rng = request.Seed.HasValue
    ? new Random(request.Seed.Value)
    : new Random();
```
若 Python 传入 `seed`，使用固定种子保证 100% 可复现。

### 4.3 PriorityQueue 排序与时间戳碰撞消除

```csharp
PriorityQueue<SimEvent, (double Time, long EventId)>
```
- .NET `PriorityQueue` 为最小堆（min-heap），最早事件优先出队。
- 优先级类型为 `(double Time, long EventId)` 的 ValueTuple。
- ValueTuple 比较规则: **先比较 Time，Time 相同再比较 EventId**。
- 入队时 `++_eventIdCounter` 前置递增保证严格单调，从而保证相同时间戳下先创建的事件先出队（FIFO）。

### 4.4 事件主循环

```csharp
while (_eventQueue.Count > 0 && _currentTime <= _request.SimulationTime)
{
    if (!_eventQueue.TryDequeue(out var evt, out var priority) || evt == null)
        continue;
    _currentTime = priority.Time;

    if (evt.Type == "PACKET_ARRIVAL")
    {
        // 排队与时延推演 + 下一包调度
    }
}
```

### 4.5 随机分布接入 (MathNet.Numerics 5.0)

**Exponential 分布** —— 包间隔时间 (`ScheduleNextPacket` L169-170):
```csharp
double rate = 1.0 / traffic.IntervalMean;
var expDist = new Exponential(rate, _rng);
double interval = expDist.Sample();
```
- 参数: `rate = 1.0 / interval_mean`（λ 速率参数）。

**Normal 分布** —— 包载荷大小 (`ScheduleNextPacket` L173-178):
```csharp
if (traffic.PayloadVariance > 0 && traffic.PayloadMean > 0)
{
    double stddev = Math.Sqrt(traffic.PayloadVariance);
    var normDist = new Normal(traffic.PayloadMean, stddev, _rng);
    payloadSize = (int)Math.Round(normDist.Sample());
    payloadSize = Math.Max(64, payloadSize);   // 下限 64 bytes
}
else
{
    payloadSize = Math.Max(64, traffic.PayloadMean);
}
```
- 触发条件: `PayloadVariance > 0 && PayloadMean > 0`（两者必须同时满足）。
- 标准差: `sqrt(PayloadVariance)`。
- 下限保护: `Math.Max(64, ...)` 防止负值或零值畸形包。

### 4.6 PACKET_ARRIVAL 排队与时延推演

```csharp
// 1. 查找链路参数
string linkKey = $"{evt.SrcId}->{evt.CurrentNodeId}";
var link = _request.Links.FirstOrDefault(l =>
    (l.Src == evt.SrcId && l.Dst == evt.CurrentNodeId)
 || (l.Src == evt.CurrentNodeId && l.Dst == evt.SrcId));
double linkDelay = link?.Delay ?? 1.0;       // 传播时延 (ms), 默认 1.0
double linkBwMbps = link?.Bw ?? 100;          // 链路带宽 (Mbps), 默认 100

// 2. 发送时延: T_tx (ms) = (PayloadSize × 8 bits) / (Bw × 10^6 bps) × 1000
double packetBits = evt.PayloadSize * 8.0;
double bandwidthBps = linkBwMbps * 1_000_000.0;
double transmitDelay = packetBits / bandwidthBps * 1000.0;

// 3. 排队时延: 基于 LinkBusyUntil 状态推演
if (!_linkBusyUntil.ContainsKey(linkKey))
    _linkBusyUntil[linkKey] = 0.0;
double queueDelay = Math.Max(0.0, _linkBusyUntil[linkKey] - _currentTime);

// 4. 推进链路忙碌时间轴
_linkBusyUntil[linkKey] = _currentTime + queueDelay + transmitDelay;

// 5. 包总时延 = 传播延迟 + 排队时延 + 发送时延
double totalDelay = linkDelay + queueDelay + transmitDelay;
```

**LinkBusyUntil 语义**: 记录每条链路"上次传输完成"的时间点。当前包到达时，若链路仍忙碌（`LinkBusyUntil > CurrentTime`），差值即为排队等待时间。发送完成后将 LinkBusyUntil 前移，为下一包提供参考。

### 4.7 峰值队列估算

```csharp
int q = (int)(queueDelay * 10);
if (!_flowPeakQueues.ContainsKey(flowKey))
    _flowPeakQueues[flowKey] = 0;
if (q > _flowPeakQueues[flowKey])
    _flowPeakQueues[flowKey] = q;
```
将排队时延按 `×10` 映射为队列深度（每 0.1ms 对应 1 单位），取每条流的历史最大值。

### 4.8 路由计算 (`ComputeRoutes`)

当前实现为**单跳路由模型**：
```csharp
// 1. 从 Links 中识别 Host→Router 邻接关系
var hostToRouter = new Dictionary<string, string>();
foreach (var link in _request.Links)
{
    bool srcIsHost = _request.Nodes.Any(n => n.Id == link.Src && n.Type == "Host");
    bool dstIsHost = _request.Nodes.Any(n => n.Id == link.Dst && n.Type == "Host");
    if (srcIsHost) hostToRouter[link.Src] = link.Dst;
    if (dstIsHost) hostToRouter[link.Dst] = link.Src;
}

// 2. 每个 Host 到任意目标节点的下一跳均为其唯一网关 Router
foreach (var hostId in hostToRouter.Keys)
{
    routes[hostId] = new Dictionary<string, string>();
    foreach (var dst in nodeIds)
    {
        if (dst == hostId) continue;
        routes[hostId][dst] = hostToRouter[hostId];
    }
}
```
- Host 只能连接一个 Router（由拓扑校验保证）。
- 所有流量经 Host → Router 一跳转发，不模拟多跳路由。

### 4.9 拓扑校验 (`TopologyValidator`)

**文件**: `Pytools/Services/TopologyValidator.cs`

两条核心规则：
1. **Host ↔ Host 直连禁令**: 若 `hostIds.Contains(link.Src) && hostIds.Contains(link.Dst)`，返回错误。
2. **Host 必须且只能连接一个 Router**: 统计每个 Host 在 Links 中出现的次数（与 Router 相邻），若 `count != 1`，返回错误。

注意: TCP 协议拦截在 Python 端 `Traffic.__init__` 中完成，而非 C# 端。

---

## 5. 自适应 CDF 降维统计算法

**位置**: `SimulatorEngine.Run()` L97-133，仿真主循环结束后执行。

### 5.1 全局统计量

```csharp
sw.Stop();                          // 停表
_allPacketDelays.Sort();            // 升序排序
int N = _allPacketDelays.Count;

globalAvg = _allPacketDelays.Average();
if (N > 1)
    globalStdDev = Math.Sqrt(_allPacketDelays.Average(d => Math.Pow(d - globalAvg, 2)));
```
标准差使用总体标准差公式（除以 N）。

### 5.2 N ≤ 100：全量模式

```csharp
x_delays.AddRange(_allPacketDelays);              // 全部排序后的时延值
for (int i = 0; i < N; i++)
    y_probabilities.Add((double)(i + 1) / N);     // 经验 CDF = (i+1)/N
```
- x: N 个原始时延点（升序）
- y: 对应累积概率 (1/N, 2/N, ..., 1.0)
- 点数 = N

### 5.3 N > 100：101 分位点降维模式

```csharp
for (int p = 0; p <= 100; p++)
{
    double quantile = p / 100.0;                       // 0.00, 0.01, ..., 1.00
    int idx = (int)Math.Round(quantile * (N - 1));
    if (idx < 0) idx = 0;                              // 下界防越界
    if (idx >= N) idx = N - 1;                         // 上界防越界
    x_delays.Add(_allPacketDelays[idx]);
    y_probabilities.Add(quantile);
}
```
- 步长: 0.01（101 个等距分位点）
- 索引公式: `Round(quantile × (N - 1))`（四舍五入取整）
- 双端 clamp 防越界: `[0, N-1]`
- 结果固定 101 个点，与 N 无关

### 5.4 CDF 结果组装

```csharp
var cdf_data = new { x_delays, y_probabilities };
```
匿名类型序列化后为 `{"x_delays": [...], "y_probabilities": [...]}`，直接嵌入响应 JSON。

---

## 6. matplotlib 后端劫持与 Base64 图像传输

### 6.1 包装脚本中的猴子补丁

**位置**: `BuildWrapperScript` 生成的临时包装脚本 L288-303

```python
import base64 as _base64, io as _io
import matplotlib
matplotlib.use('Agg')                # ★ 强制非交互式后端（必须在 import pyplot 之前）
import matplotlib.pyplot as _plt

def _pytools_show(*_args, **_kwargs):
    for _fignum in _plt.get_fignums():
        _fig = _plt.figure(_fignum)
        _buf = _io.BytesIO()
        _fig.savefig(_buf, format='png', dpi=100, bbox_inches='tight')
        _buf.seek(0)
        _b64 = _base64.b64encode(_buf.read()).decode('ascii')
        print('##PLOT_DATA_START##' + _b64 + '##PLOT_DATA_END##')
        _buf.close()
        _plt.close(_fig)
_plt.show = _pytools_show            # ★ 全局替换 plt.show()
```

**关键特性**:
- `matplotlib.use('Agg')` 在 `import pyplot` 之前调用，禁止 GUI 窗口弹出。
- 拦截所有 `plt.show()` 调用，遍历所有活跃 Figure。
- 每个 Figure: `savefig(BytesIO, format='png', dpi=100, bbox_inches='tight')` → Base64 编码 → stdout 打印。
- 打印后立即 `plt.close(_fig)` 释放内存。
- 当前实现未显式配置中文字体（`SimHei` 等），中文标题/标签依赖系统默认字体；也未使用 `PercentFormatter` 处理纵轴百分比。

### 6.2 C# 端图像重建

**位置**: `MainViewModel` L136-153

```csharp
_pythonService.PlotReceived += data =>
    System.Windows.Application.Current.Dispatcher.Invoke(() =>
    {
        var bmp = new BitmapImage();
        bmp.BeginInit();
        bmp.CacheOption = BitmapCacheOption.OnLoad;
        bmp.StreamSource = new MemoryStream(data);
        bmp.EndInit();
        bmp.Freeze();                                   // ★ 跨线程安全
        var plot = new PlotModel
        {
            Title = $"Plot {PlotList.Count + 1}",
            ImageSource = bmp,
            RawImageData = data                          // 保留原始字节用于保存
        };
        PlotList.Add(plot);
        SelectedPlot = plot;
    });
```
- `Freeze()` 使 `BitmapImage` 可跨线程安全访问。
- `RawImageData` 保留 PNG 原始字节，供 `SavePlotCommand` / `SaveAllPlotsCommand` 直接 `File.WriteAllBytes`。

---

## 7. 接口与数据流协议 (JSON Schema)

### 7.1 Python → C# (配置下发请求)

`##SIM_START## {JSON} ##SIM_END##`

```json
{
  "simulation_time": 500,
  "seed": 42,
  "summary_only": false,
  "nodes": [
    {"id": "H1", "type": "Host"},
    {"id": "H2", "type": "Host"},
    {"id": "R1", "type": "Router"}
  ],
  "links": [
    {"src": "H1", "dst": "R1", "bw": 100, "delay": 1.0},
    {"src": "H2", "dst": "R1", "bw": 100, "delay": 2.0}
  ],
  "traffic": [
    {
      "src": "H1",
      "dst": "H2",
      "type": "UDP",
      "interval_mean": 1.0,
      "payload_mean": 512,
      "payload_variance": 32
    }
  ]
}
```

**字段说明**（对应 C# `SimulationRequest` 类）:

| 字段 | 类型 | 说明 |
|------|------|------|
| `simulation_time` | `int` | 仿真时长 (ms) |
| `seed` | `int \| null` | 随机种子，null 则使用系统时间 |
| `summary_only` | `bool` | true 时 flows 返回空字典 |
| `nodes` | `list` | `id: string`, `type: "Host" \| "Router"` |
| `links` | `list` | `src`, `dst`, `bw: int` (Mbps), `delay: float` (ms) |
| `traffic` | `list` | `src`, `dst`, `type`, `interval_mean: float`, `payload_mean: int`, `payload_variance: float` |

注意：当前版本**无** `output_config` 字段。

### 7.2 C# → Python (仿真结果响应)

通过 `Process.StandardInput.WriteLine(jsonString)` 注入 Python 的 `stdin.readline()` 阻塞点。

**成功响应 (summary_only=false)**:
```json
{
  "status": "success",
  "total_packets": 1513,
  "execution_time_sec": 0.045,
  "global_avg_delay_ms": 3.835,
  "global_std_dev_ms": 0.523,
  "cdf_data": {
    "x_delays": [1.0, 2.5, 3.8],
    "y_probabilities": [0.0, 0.5, 1.0]
  },
  "flows": {
    "H1->H2": {
      "avg_delay_ms": 12.45,
      "loss_rate": 0.0,
      "peak_queue": 42
    }
  }
}
```

**成功响应 (summary_only=true)**:
```json
{
  "status": "success",
  "total_packets": 1513,
  "execution_time_sec": 0.045,
  "global_avg_delay_ms": 3.835,
  "global_std_dev_ms": 0.523,
  "cdf_data": { ... },
  "flows": {}
}
```
`flows` 为空字典 `{}`，避免大规模压测时传输海量逐流数据。

**失败/异常响应**:
```json
{
  "status": "error",
  "message": "拓扑校验失败：主机 'H1' 未连接或连接了多个路由器。它必须且只能连接一个默认网关。"
}
```

注意：当前版本**无** `routing_matrix` 字段。`loss_rate` 当前恒为 `0.0`（丢包逻辑尚未实现）。

---

## 8. WPF MVVM 数据流与线程调度

### 8.1 核心 ViewModel

**文件**: `Pytools/ViewModels/MainViewModel.cs`

```csharp
public class MainViewModel : INotifyPropertyChanged
{
    public ObservableCollection<string> ConsoleLines { get; } = new();
    public ObservableCollection<VariableEntry> VariableList { get; } = new();
    public ObservableCollection<PlotModel> PlotList { get; } = new();
    public ObservableCollection<DocumentModel> Documents { get; } = new();
    private readonly PythonExecutionService _pythonService;
    private readonly FileWatcherService _fileWatcher;
    // ...
}
```

### 8.2 线程调度 (Dispatcher)

所有 `PythonExecutionService` 事件的回调均通过 `Application.Current.Dispatcher.Invoke` 封送到 UI 线程：

```csharp
// 文本行:
_pythonService.LineReceived += line =>
    Dispatcher.Invoke(() => ConsoleLines.Add(line));

// 变量字典:
_pythonService.VariablesReceived += vars =>
    Dispatcher.Invoke(() => { foreach (var v in vars) VariableList.Add(v); });

// Base64 图表:
_pythonService.PlotReceived += data =>
    Dispatcher.Invoke(() => { /* BitmapImage 构建 + PlotList.Add */ });
```

### 8.3 执行生命周期

```csharp
private void ExecuteRun()
{
    // 1. 自动保存当前文档 → File.WriteAllText
    // 2. 清空: VariableList.Clear(); PlotList.Clear(); SelectedPlot = null;
    // 3. 发射: _ = _pythonService.ExecuteAsync(ActiveDocument.FilePath);
    //    (fire-and-forget, 不 await)
}
```

### 8.4 文件监听与防抖

**文件**: `Pytools/Services/FileWatcherService.cs`

```csharp
_fileWatcher = new FileWatcherService("*.py", 200);  // 过滤 *.py, 200ms 防抖
_fileWatcher.FilesChanged += () => TreeRefreshRequested?.Invoke();
```

防抖机制: 每次文件变更事件重置 `System.Timers.Timer`，200ms 无新事件后才通过 `Dispatcher.Invoke` 触发目录树重建。

### 8.5 图表保存

- **单图保存** (`SavePlotCommand`): `File.WriteAllBytes(path, SelectedPlot.RawImageData)`
- **批量保存** (`SaveAllPlotsCommand`): 遍历 `PlotList`，逐个 `File.WriteAllBytes(Path.Combine(folder, $"Plot_{i+1}.png"), ...)`

---

## 9. 技术栈与依赖

| 组件 | 技术 | 版本 |
|------|------|------|
| 运行时 | .NET 8.0 Windows (WPF) | net8.0-windows |
| 代码编辑器 | AvalonEdit | 6.3.1.120 |
| 停靠布局 | Dirkster.AvalonDock | 4.74.1 |
| 停靠主题 | Dirkster.AvalonDock.Themes.VS2013 | 4.74.1 |
| UI 样式 | MaterialDesignThemes | 5.3.2 |
| UI 框架 | ModernWpfUI | 0.9.6 |
| 科学计算 | MathNet.Numerics | 5.0.0 |
| Python 端 | matplotlib (Agg 后端) | Python 3.x |

---

## 10. 版本迭代记录

| 版本 | 日期 | 主要变更 |
|------|------|----------|
| V5.2 | 2026-05-19 | 基于全量代码库逆向审计修正：移除旧文档中不存在的 `IRoutingStrategy`、对象池、`routing_matrix`、`output_config`、`Packet` 类等设计；对齐实际 IPC 三通道解析、包装脚本生成、PriorityQueue 泛型签名、CDF 101 分位点降维算法、`_translate_keys` 中文字段映射等真实实现细节。 |
